#!/bin/sh
# distrobox_binary
# name: nimbus
if [ -z "${CONTAINER_ID}" ]; then
	if ! podman container exists gamebox ; then
		distrobox create -i ghcr.io/ublue-os/bazzite-arch -n gamebox -Y
	fi
	exec /usr/bin/distrobox-enter  -n gamebox -- 		 /usr/sbin/steam  "$@"
else
	exec /usr/sbin/steam "$@"
fi
